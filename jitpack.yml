# JitPack build configuration
# This file ensures JitPack builds your project correctly

jdk:
  - openjdk21

before_install:
  - sdk install java 21.0.1-open || true
  - sdk use java 21.0.1-open

install:
  - ./gradlew clean build publishToMavenLocal -x test